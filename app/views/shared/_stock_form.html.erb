<%= render "shared/errors", obj: @stocks %>
<table class="attr">
  <tr>
    <th><%= form.label :name, "名前" %></th>
    <td><%= form.text_field :name %></td>
  </tr>
  <tr>
    <th><%= form.label :number, "個数"  %></th>
    <td><%= form.text_field :number %></td>
  </tr>
  <tr>
    <th><%= form.label :assort, "種類"  %></th>
    <td><%= form.select :assort, options_for_select(@assorts) %></td>
  </tr>
</table>
